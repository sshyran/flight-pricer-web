<div class="row">
  <div class="col">
  </div>
  <div class="col-md-10 align-self-center">
    <ul class="nav nav-tabs">
      <li class="nav-item">
        {{#link-to "search-one-way" class="nav-link"}}One Way{{/link-to}}
      </li>
      <li class="nav-item">
        {{#link-to "search-round-trip" class="nav-link"}}Round trip{{/link-to}}
      </li>
      <li class="nav-item">
        {{#link-to "search-multi-city" class="nav-link"}}Multi City{{/link-to}}
      </li>
    </ul>

    <div class="tab-content search-flights-block">
      <div class="tab-pane active">
        <form>
          {{#if isOneWay}}
            {{one-way-search}}
          {{/if}}

          {{#if isRoundTrip}}
            {{round-trip-search}}
          {{/if}}

          {{#if isMultiCity}}
            {{multi-city-search}}
          {{/if}}
          <div class="row">
            <div class="col-10"></div>
            <div class="col-2 align-self-end">
              <button type="submit" class="btn btn-primary">Search</button>

            </div>
          </div>

        </form>
      </div>
    </div>

  </div>
  <div class="col"></div>
</div>







{{yield}}