import travelClass from 'flight-pricer-web/utils/travel-class';
import { module, test } from 'qunit';

module('Unit | Utility | travel class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = travelClass.create();
  assert.ok(result);
});
