<select {{action 'change' on='change'}} class="custom-select">
  {{#each content key="@index" as |item|}}
    <option value="{{item.value}}" selected={{is-equal item.value selectedValue}}>
      {{item.text}}
    </option>
  {{/each}}
</select>
